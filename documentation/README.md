# Decluttify
Repository for ISPW project for application Decluttify

Use Cases Implemented
Make Offer

Configuration
To modify application settings (Persistence type, DB credentials, Environment mode), update the configuration file: src/main/resources/config.properties
